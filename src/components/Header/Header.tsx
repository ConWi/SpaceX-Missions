import React from 'react';
import style from './Header.module.css'

const Header = () => (
    <div className={style.headerContainer}>
        <div className={style.headerTitle}>Launches</div>
    </div>
);

export default Header;